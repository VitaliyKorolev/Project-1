#include <iostream>
#include <vector>
using namespace std;
void gnomesort( vector <int> &A) {
    int N=A.size();
    int i = 0;
	int tmp;
	 while (i < N) {
          if (i == 0 || A[i - 1] <= A[i])
            ++i;
          else {
            tmp = A[i];
            A[i] = A[i - 1];
            A[i - 1] = tmp;
            --i;
         }
	}
}
void outinconsole( vector <int> &A) {
    int n=A.size();
    for (int i=0; i < n; i++){  
    cout << A[i]<<" ";
    }
}
int main() {
 int n;
 cin >> n ;
 vector <int> a(n);
 for (int i=0; i < n; i++){ 
  cin >> a[i];
 }
gnomesort(a);
outinconsole( a);
return 0;
}
