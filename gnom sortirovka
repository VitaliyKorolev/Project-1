#include <iostream>
#include <vector>
using namespace std;
void gnomesort(int N, vector <int> &A) {
	int i = 0;
	int tmp;
	 while (i < N) {
          if (i == 0 || A[i - 1] <= A[i])
            ++i;
          else {
            tmp = A[i];
            A[i] = A[i - 1];
            A[i - 1] = tmp;
            --i;
         }
	}
}
int main() {
 int n;
 cin >> n ;
 vector <int> a(n);
 for (int i=0; i < n; i++){ 
  cin >> a[i];
 }
gnomesort(n,a);
for (int i=0; i < n; i++){ 
  cout << a[i]<<" ";
 }
return 0;
}
